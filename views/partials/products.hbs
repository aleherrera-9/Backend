{{#if DB_PRODUCTS}}

    <h2 class="text-white">Lista de Productos</h2>
    <table class="table">
        <tr>
            <th scope="col"></th>
            <th scope="col">Producto</th>
            <th scope="col">Precio</th>
        </tr>
        {{#each DB_PRODUCTS}}
        <tbody id="element">
         {{!-- Espacio para cargar los productos --}}
        </tbody>
        {{/each}}
    </table>

{{else}}
<h1> NO SE ENCONTRARON PRODUCTOS </h1>
{{/if}}